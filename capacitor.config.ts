import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gochar_patrika.app',
  appName: 'gocharPatrika',
  webDir: 'dist/gochar-patrika',
  bundledWebRuntime: false
};

export default config;
